{
  "private": true,
  "name": "mini-file-encryptor",
  "version": "0.0.1",
  "description": "Mini File Encryptor - a minimalistic offline file encryptor.",
  "author": "GJTiquia",
  "license": "MIT",
  "workspaces": {
    "packages": [
      "packages/*",
      "apps/*"
    ],
    "nohoist": [
      "**"
    ]
  },
  "scripts": {
    "test": "turbo run test",
    "build": "turbo run build",
    "clean": "turbo run clean && rm -rf node_modules && rm -rf yarn.lock"
  },
  "dependencies": {
    "turbo": "^1.10.16"
  }
}